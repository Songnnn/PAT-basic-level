#include<stdio.h>
#include<string.h>
int myatoi(char x)
{
    return x-48;
}
char get(int s)
{
    switch(s)
    {
        case 0:return '0';
        case 1:return '1';
        case 2:return '2';
        case 3:return '3';
        case 4:return '4';
        case 5:return '5';
        case 6:return '6';
        case 7:return '7';
        case 8:return '8';
        case 9:return '9';
        case 10:return 'J';
        case 11:return 'Q';
        case 12:return 'K';
    }
}
int main()
{
    char A[101];
    char B[101];
    char C[101];
    scanf("%s %s",A,B);

    int A_length=strlen(A);
    int B_length=strlen(B);

    int i,j,k;
    for( i=0 ; i<101 ; i++ ) C[i]='\0';
    int count=1;
    for( i=A_length-1,j=B_length-1,k=0 ; i>=0 && j>=0 ; i--,j--,k++ )
    {
        int a=myatoi(A[i]);
        int b=myatoi(B[j]);
        int s=0;
        if( count%2==0 )
        {
            s=b-a;
            if( s<0 ) s+=10;
        }
        else s=(a+b)%13;
        C[k]=get(s);
        count++;
    }

    if( j>0 )
    {
        for( ; j>=0 ; j--,k++ ) C[k]=B[j];
    }
    else if( i>0 )
    {
        for( ; i>=0 ; i--,k++ ) C[k]=A[i];
    }

    int C_length=strlen(C);
    for( i=C_length-1 ; i>=0 ; i-- ) printf("%c",C[i]);
    return 0;
}





