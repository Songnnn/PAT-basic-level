#include<bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    scanf("%d",&n);

    double p,sum=0;
    for( int i=0 ; i<n ; i++ )
    {
        scanf("%lf",&p);
        sum=sum+p*(i+1)*(n-i);
    }

    printf("%.2lf",sum);

    return 0;

}
